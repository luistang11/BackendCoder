<form action="/register" method="post">
  <label for="first_name">Nombre</label><br/>
  <input name="first_name "type="text" /><br/>
  <label for="last_name">Apellido</label><br/>
  <input name=" last_name"type="text" /><br/>
  <label for="email" >Email</label><br/>
  <input tname="email"ype="email" /><br/>
  <label for="age">Edad</label><br/>
  <input name="age"type="number" min="0" /><br/>
  <label for="password">Contraseña</label><br/>
  <input name="password"type="password" minlength="6" /><br>
  <button type="submit" value="submit">Crear usuario</button>
</form>

<script>
  document.querySelector("form").addEventListener("submit", function(event) {
    event.preventDefault();
    
    // Hacer una petición POST con los datos del formulario
    fetch("/register", {
      method: "post",
      body: new FormData(event.target)
    })
    .then(()=> {
      console.log(new FormData(event.target))
      // Redirigir a otra página
      window.location.href = "/login";
    });
  });
</script>

